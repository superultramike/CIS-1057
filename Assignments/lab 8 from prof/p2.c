#include<stdio.h>

char COLOR_CODES[10][7] = {"black", "brown", "red","orange", "yellow", "green", "blue", "violet", "gray","white"};


int main() {
    printf("Enter the colors of the resistor's three bands, beginning with the band nearest the end. Type the colors in lowercase letters only, NO CAPS.");

    int first=0;
    int second=0;
    int third=0'

    &first

    int * a // value of a can be changed  a=a+1;

    int A[]={"hi"}; // value of a cannot be changed   XX not possible A=A+1
    int B[]={"hi"};

    while(true)
    {

        printf("Band 1 =>");
        char[7] f_string;
        scanf("%s", f_string);
        first=value(f_string);

        printf("Band 2 =>");
        char[7] s_string;
        scanf("%s", s_string);
        first=value(f_string);

        printf("Band 3 =>");
        char[7] t_string;
        scanf("%s", t_string);
        first=value(f_string);

        //calculation of finding index of color
        double= (first*10+second)* pow(10, third);

        // strcmp();

        //final value of the resister
    }
    return 0;
}

void search() {

}

#include<stdio.h>


void suffix(char * first_string, char * second_string, char* output)
{

    // last position of first string
    // last position of second string

    //loop that starts from the last position
    // keep moving backward unless you get unmatched character.

    // starting position of the suffix

    // copy to the output from the start of suffix to the end

     // globally
    //internally



}

int main()
{
    char fs[20];
    char ss[20];

    char output[20];
    printf("enter first string: \n");
    scanf("%s", fs);
    printf("enter second string: \n");
    scanf("%s", ss);


    suffix(fs,ss,output);


    printf("%s", output);
}
